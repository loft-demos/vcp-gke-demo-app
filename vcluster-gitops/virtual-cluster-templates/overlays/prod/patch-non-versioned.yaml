apiVersion: management.loft.sh/v1
kind: VirtualClusterTemplate
metadata:
  name: dummy-name-to-be-overridden
spec:
  template:
    helmRelease:
      chart:
        version: 0.30.0
  parameters:
    - variable: sleepAfter
      label: Sleep After Inactivity (minutes)
      options:
        - '30'
        - '45'
        - '60'
        - '120'
      defaultValue: '45'
    - variable: k8sVersion
      label: k8sVersion
      description: Please select Kubernetes version
      options:
        - v1.34.2
        - v1.33.6
        - v1.32.10
        - v1.31.14
      defaultValue: v1.33.6
